pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{ts,js,svelte}"
      run: bun run check
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: bun run typecheck
    build:
      run: bun run build

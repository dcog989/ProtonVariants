pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{ts,js,svelte}"
      run: bun run check
      stage_fixed: true

pre-push:
  commands:
    scrape:
      run: bun run scrape
    build:
      run: bun run build

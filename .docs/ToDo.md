# To Do: FeedMee

- when switching to another feed or folder, it should always have its content scrolled to the top - currently it remembers the scroll position of the previous feed/folder??

---

## Known Issues

- ?
- persistent issue that has resisted multiple attempts to fix - ui css is sometimes not fully loaded when running from 'bun run dev'. general styling is loaded but not layout CSS so that each panel is stacked on top of each other. HMR / Vite is not correctly loading CSS??
- remove TypeScript override when svelte supports v7 - <https://github.com/sveltejs/language-tools/issues/3063>

---

## Next Version

- ?
